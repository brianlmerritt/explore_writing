# Duo story example remote folder

This is an example of the output of a slightly earlier version of explore_writing.

The main difference is reviews.tsv has the scores in json (the current version is one column per metric for spreadsheet ease of use) and no top_writing.tsv

I didn't include all the output because that is a lot of AI generated stuff, but did copy in a couple of the better samples

`duo.md` in this folder is my original version for comparison